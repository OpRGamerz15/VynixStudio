# This repository is a static GitHub Pages site and does not read runtime secrets.
# Keep credentials out of HTML, JavaScript, Git, and client-side build artifacts.
# A future Control Panel backend should define its variables in its own deployment.

# PUBLIC_SITE_URL=https://www.vynixstudio.indevs.in
# API_ORIGIN=https://api.example.com
# TRUSTED_PROXY_IPS=192.0.2.10/32
# CDN_WAF_REQUEST_ID_HEADER=CF-Ray
